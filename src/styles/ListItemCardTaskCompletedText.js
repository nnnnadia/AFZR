import { ListItemText, styled } from '@mui/material';

const ListItemCardTaskCompletedText = styled(ListItemText)`
  color: #a6a6a6;
  text-decoration: line-through;
`;

export default ListItemCardTaskCompletedText;
